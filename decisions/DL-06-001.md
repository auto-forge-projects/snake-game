# DL-06-001: UI/UX — tek ekran, Canvas + aria-live erişilebilirlik katmanı

- Tarih: 2026-07-19
- Faz: 6 — UI/UX
- Mod: AUTOPILOT
- Karar: `docs/06-uiux/06-uiux.md` üretildi — tek ekran (Canvas ızgara + skor + başlat/oyun-sonu katmanları), Faz 5 mimarisindeki fixed-step/rAF ayrımına birebir uyumlu ana akış (Mermaid sequenceDiagram) çizildi.
- Değerlendirilen alternatifler: (1) Canvas + ayrı DOM `aria-live` bölgeleri (skor/oyun-sonu). (2) Tamamen DOM/CSS-grid tabanlı arayüz (Faz 4'te elenen render seçeneğiyle çelişir).
- Gerekçe: (1) seçildi — Faz 4/5'te kararlaştırılan Canvas render ile tutarlı; erişilebilirlik boşluğu (Canvas'ın kendisi ekran okuyucuya görünmez) ayrı `aria-live` DOM öğeleriyle (skor: polite, oyun-sonu: assertive) kapatıldı, NFR-5 karşılanıyor.
- Riskler: Düz-renk minimal görsel stil (DL-03-001 varsayımı) kullanıcı beklentisiyle uyuşmayabilir — düşük risk, yalnızca CSS ile sonradan değiştirilebilir.
- Geri alınabilirlik: Yüksek (yalnız dokümantasyon; `/pipeline-back 6` ile tekrar üretilebilir).
- İnsan onayı: Gerekmiyor (AUTOPILOT — DL'ye yazıldı, kullanıcı sonradan denetler).
- Varsayım mı?: Hayır (doğrudan Faz 3/4/5 kararlarından türetildi, yeni varsayım yok).
