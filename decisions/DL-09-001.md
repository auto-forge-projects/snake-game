# DL-09-001: Development — TDD ile 9 task, sıfır-bağımlılık istemci + Express statik sunucu

- Tarih: 2026-07-19
- Faz: 9 — Development
- Mod: AUTOPILOT
- Karar: `src/server.js` (Express static + `/health` + güvenlik başlıkları) ve `public/{index.html,styles.css,game.js,storage.js}` (fixed-step oyun mantığı + rAF render ayrımı, canvas çizim, klavye kontrolü, localStorage sertleştirilmiş wrapper) TASK-001..009 sırasıyla TDD (red→green commit çifti) ile üretildi. Faz 7'nin SEC-1..8 önlemleri (güvenlik başlıkları, kilitli `express.static`, storage parseInt/NaN/aralık koruması, injection-free DOM/`textContent`) TASK-008'de koda gömüldü. TASK-009 mevcut bootstrap köprüsünü (TASK-008'deki `document` kablolaması) entegrasyon testleriyle kapsama aldı — yeni davranış eklemedi, yalnız coverage'ı tamamladı.
- Değerlendirilen alternatifler: (1) her FR/SEC için ayrı red/green commit çifti (seçildi — izlenebilir, küçük diff'ler). (2) tüm oyun mantığını tek commit'te yazıp sonra testlerle doğrulama (TDD disiplinini bozar, Faz 8 planına aykırı).
- Gerekçe: (1) seçildi — Faz 8 planının öngördüğü TDD sırasını korur, her commit tek bir FR/SEC'e izlenebilir, `verify-gate.mjs` mechanical kapısı (TDD commit-kanıtı) bunu doğrudan doğrular.
- Riskler: Tarayıcı-taraflı `document`/`requestAnimationFrame` kodu jsdom olmadan (sıfır-bağımlılık hedefi) sahte-DOM ile test edildi — gerçek tarayıcı DOM API yüzeyinin tamamını kapsamaz (düşük risk, kritik akışlar TASK-009 entegrasyon testleriyle uçtan uca sürüldü).
- Geri alınabilirlik: Orta (kod; `git revert` ile task bazında geri alınabilir, testler regresyonu yakalar).
- İnsan onayı: Gerekmiyor (AUTOPILOT — DL'ye yazıldı, kullanıcı sonradan denetler).
- Varsayım mı?: Hayır (uygulama doğrudan `docs/03-requirements.md` FR/NFR ve `docs/07-security.md` SEC maddelerinden türetildi).

## Kalite kapısı raporu
- "Build yeşil" → `npm test` 39/39 ✅ GEÇTİ.
- "Coverage ≥%70" → `npm run test:coverage`: all files 99.14% line / 95.83% branch / 96.15% funcs ✅ GEÇTİ.
