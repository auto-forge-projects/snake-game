{
  "name": "snake-game",
  "version": "0.1.0",
  "private": true,
  "description": "Klasik Snake — istemci-taraflı Canvas oyunu + Node/Express statik sunucu (sıfır sunucu-durumu)",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
